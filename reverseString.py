# Reverse String
def reverseString(self, s: List[str]) -> None:
        """
        Do not return anything, modify s in-place instead.
        """
        #tmp = ""
        #l = len(s)
        #for i in range(l):
            #tmp += s[i]
            
        #tmp = tmp[::-1]
        
        #for i in range(l):
            #s[i] = tmp[i]
            
        s.reverse()
